/**
 tuples (if we want to use different types in one array) 
 **/
const names: string[] = ['abul', 'babul', 'kabul'];

const numbers :number[] = [2,5,6,9,12]

let mixed: [string, number, string, number, boolean] = ['sujon', 55, 'Chitagong', 3,true];
mixed[4] = false;